export async function fetchAppointmentsFromApi(role, userId) {
  // to be replaced with real API call
  return [];
}

export async function createAppointmentInApi(form, role, userId) {
    // to be completed when we work on ture data integration
  return { ok: true };
}

export async function updateAppointmentStatusInApi(id, status, note) {
  // to be completed when we work on ture data integration
  return { ok: true };
}
