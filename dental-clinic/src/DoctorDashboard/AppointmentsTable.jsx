import React from "react";

const AppointmentsTable = () => {
  return (
    <div style={{
      background: "white",
      padding: "20px",
      borderRadius: "12px",
      boxShadow: "0 4px 12px rgba(0,0,0,0.1)"
    }}>
      <h3>Appointments Table</h3>
      <p>This is a placeholder until real data is added.</p>
    </div>
  );
};

export default AppointmentsTable;
